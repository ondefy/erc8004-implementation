# Node modules
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ZK Build artifacts - Strategic Ignoring
# We commit ONLY the essential files needed for proof generation in production.
# Setup/intermediate files remain ignored for security and repo cleanliness.

# Keep build directory structure and documentation
!build/
!build/README.md

# ==================== PORTFOLIO REBALANCING CIRCUIT ====================
# Required for production proof generation (COMMIT THESE)
!build/rebalancing_js/
!build/rebalancing_js/*.js
!build/rebalancing_js/*.wasm
!build/rebalancing_final.zkey
!build/verification_key.json

# ==================== ZYFI REBALANCER VALIDATION CIRCUIT ====================
# Required for production proof generation (COMMIT THESE)
!build/rebalancer-validation/
!build/rebalancer-validation/*.json
!build/rebalancer-validation/rebalancer_validation_final.zkey
!build/rebalancer-validation/rebalancer-validation_js/
!build/rebalancer-validation/rebalancer-validation_js/*.js
!build/rebalancer-validation/rebalancer-validation_js/*.wasm

# ==================== IGNORED: Setup/Intermediate Files ====================
# Powers of Tau (only needed during setup, not runtime)
build/*.ptau
build/rebalancer-validation/*.ptau

# Intermediate proving keys (only needed during setup)
build/*_0000.zkey
build/*_0001.zkey
build/rebalancer-validation/*_0000.zkey
build/rebalancer-validation/*_0001.zkey

# Debug/development files
build/*.r1cs
build/*.sym
build/rebalancer-validation/*.r1cs
build/rebalancer-validation/*.sym
build/*.wasm

# Runtime generated files (created during proof generation)
build/*.wtns
build/proof.json
build/public.json
build/temp_input.json
build/rebalancer-validation/*.wtns
build/rebalancer-validation/proof.json
build/rebalancer-validation/public.json
build/rebalancer-validation/temp_rebalancer_input.json

# Deposit validation subdirectory (experimental/unused)
build/rebalancer-validation/deposit-validation/

# ==================== RUNTIME DATA DIRECTORIES ====================
# Keep directory structure but ignore runtime-generated content
data/*
!data/.gitkeep
validations/*
!validations/.gitkeep

# Foundry / Forge
contracts/cache/
contracts/out/
contracts/broadcast/
contracts/lib/

# Keep foundry config and scripts
!contracts/foundry.toml
!contracts/script/
!contracts/src/

# Environment variables & secrets
.env
.env.*
!.env.example

# Agent registration sensitive files
latest-ipfs-cid.txt
latest-agent-id.txt
.private_key

# IDE specific files
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Logs
logs/
*.log

# OS specific
Thumbs.db
.AppleDouble
.LSOverride

# Temporary files
tmp/
temp/
*.tmp

# Circom source code (Rust project, not needed in production)
circom/

# Circom cache
.cache/

# Test coverage
coverage/
lcov.info

# Documentation build
docs/_build/

# Dist directory
dist/